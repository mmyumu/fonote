# Allowlist: the context is limited to the server and the lineup it reads. The rest of the
# repository — including `.tooling/` and its sixteen gigabytes of Android tools, and the build
# machine's `.env` — has no business in an image.
*
!backend/server.py
!backend/espn.py
!android/app/src/main/assets/match.json
